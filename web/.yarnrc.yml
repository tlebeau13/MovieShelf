# Yarn 4 defaults to Plug'n'Play. Next.js supports it, but the node_modules
# linker keeps the eventual Dockerfile conventional: a cached dependency layer
# built from package.json + yarn.lock, the same shape api/ and analytics/ use.
nodeLinker: node-modules
