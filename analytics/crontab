# supercronic crontab — one line per scheduled job.
#
# Times are UTC (the container has no TZ set). supercronic logs each run to
# stdout, so `docker compose logs analytics` is the whole story.
#
# Empty on purpose since #6: the `hello` scaffolding job is gone, and the real
# cadences arrive with #12-#14. supercronic keeps running with nothing to do,
# which is what keeps the container from restart-looping until then.
